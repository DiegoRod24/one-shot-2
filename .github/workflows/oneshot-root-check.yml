name: ONE SHOT continuity check

on:
  push:
    branches:
      - main
      - 'agent/oneshot-*'
    paths:
      - 'one-bootstrap-v643.js'
      - 'one-v644-core.js'
      - 'one-v645-core.js'
      - 'service-worker.js'
      - 'version.json'
      - '.github/workflows/oneshot-root-check.yml'
  pull_request:
    paths:
      - 'one-bootstrap-v643.js'
      - 'one-v644-core.js'
      - 'one-v645-core.js'
      - 'service-worker.js'
      - 'version.json'

jobs:
  continuity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Syntax
        run: |
          node --check one-bootstrap-v643.js
          node --check one-v644-core.js
          node --check one-v645-core.js
          node --check service-worker.js
      - name: Continuity rules
        run: |
          grep -q "PANEL','BANNER','PINTA" one-v644-core.js
          grep -q 'one-v645-core.js' service-worker.js
          grep -q 'RESCATE DE VIDEO' version.json
          grep -q "window.candidato=''" one-v645-core.js
          grep -q 'AUTO_ORIENTATION_V645' one-v645-core.js
          grep -q 'v645PartySprite' one-v645-core.js
          grep -q 'DiegoRod24/one-shot-2/main/version.json' one-bootstrap-v643.js
          grep -q 'oneshotDismissedBuild' one-bootstrap-v643.js
          ! grep -q "removeItem('oneshotDismissedBuild')" one-bootstrap-v643.js
          ! grep -q "localStorage.setItem('oneshotAppliedBuild',BUILD)" one-bootstrap-v643.js
          ! grep -q "const TYPES=new Set(\['PANEL','MURAL','BANNER'\])" one-v644-core.js
