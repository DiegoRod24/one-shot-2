const fs=require("fs"),path=require("path");const root=process.cwd(),android=path.join(root,"android");if(!fs.existsSync(android)){console.warn("No existe android. Ejecuta npx cap add android.");process.exit(0)}const pkg=path.join(android,"app/src/main/java/pe/oneshot/evidence");fs.mkdirSync(pkg,{recursive:true});fs.copyFileSync(path.join(root,"APK/native-android/OneShotSharePlugin.java"),path.join(pkg,"OneShotSharePlugin.java"));const xmlDir=path.join(android,"app/src/main/res/xml");fs.mkdirSync(xmlDir,{recursive:true});fs.writeFileSync(path.join(xmlDir,"file_paths.xml"),'<paths xmlns:android="http://schemas.android.com/apk/res/android"><cache-path name="oneshot_share" path="oneshot-share/" /></paths>');const manifest=path.join(android,"app/src/main/AndroidManifest.xml");let mx=fs.readFileSync(manifest,"utf8");if(!mx.includes("androidx.core.content.FileProvider"))mx=mx.replace("</application>",'<provider android:name="androidx.core.content.FileProvider" android:authorities="${applicationId}.fileprovider" android:exported="false" android:grantUriPermissions="true"><meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" /></provider></application>');fs.writeFileSync(manifest,mx);fs.writeFileSync(path.join(pkg,"MainActivity.java"),`package pe.oneshot.evidence;\n\nimport android.os.Bundle;\nimport com.getcapacitor.BridgeActivity;\n\npublic class MainActivity extends BridgeActivity {\n  @Override\n  public void onCreate(Bundle savedInstanceState) {\n    registerPlugin(OneShotSharePlugin.class);\n    super.onCreate(savedInstanceState);\n  }\n}\n`);console.log("[ONE SHOT] Plugin OneShotShare registrado en MainActivity.");
