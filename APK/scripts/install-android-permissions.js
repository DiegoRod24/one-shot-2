const fs=require("fs"),path=require("path");const m=path.join(process.cwd(),"android/app/src/main/AndroidManifest.xml");if(!fs.existsSync(m)){console.warn("AndroidManifest no encontrado. Ejecuta npx cap add android.");process.exit(0)}let xml=fs.readFileSync(m,"utf8");const lines=['<uses-permission android:name="android.permission.CAMERA" />','<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />','<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />','<uses-permission android:name="android.permission.INTERNET" />','<uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />','<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />','<uses-feature android:name="android.hardware.camera" android:required="false" />','<uses-feature android:name="android.hardware.location.gps" android:required="false" />'];for(const line of lines){const name=(line.match(/android:name="([^"]+)"/)||[])[1];if(name&&!xml.includes(`android:name="${name}"`))xml=xml.replace("<application",line+"\n    <application")}xml=xml.replace(/android:label="[^"]*"/,'android:label="ONE SHOT"');fs.writeFileSync(m,xml);console.log("[ONE SHOT] Permisos aplicados.");
