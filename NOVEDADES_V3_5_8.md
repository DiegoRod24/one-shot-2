# ONE SHOT v3.5.8 · SPLIT DATE TIME + COORDINATES

## Excel
- En la hoja **EVIDENCIAS**, `FECHA` y `HORA` ahora van en columnas separadas.
- En la hoja **EVIDENCIAS**, `LATITUD` y `LONGITUD` ahora van en columnas separadas.
- Se conserva el hipervínculo a Google Maps en Dirección, Latitud, Longitud y Mapa.
- `PRECISIÓN` queda en su propia columna visual.
